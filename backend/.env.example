PORT=3000
JWT_SECRET=your_secret_here
JWT_EXPIRES_IN=24h

# Storage: memory | postgres (default: memory)
STORAGE=memory

# PostgreSQL (used when STORAGE=postgres, pelo knex e pelo docker-compose)
PG_HOST=localhost
PG_PORT=5433
PG_USER=postgres
PG_PASSWORD=sua_senha_aqui
PG_DATABASE=SPS_TEST
